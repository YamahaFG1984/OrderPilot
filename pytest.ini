[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py
testpaths = tests
addopts = -q
# WhiteNoise 直接从各 app 的 static 目录提供文件，测试中没有 collectstatic 生成的 STATIC_ROOT
filterwarnings =
    ignore:No directory at:UserWarning
